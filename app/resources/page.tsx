import React from 'react';
import { ResourcesPage } from '../components/ResourcesPage';

// Server component route that renders the typed ResourcesPage client component
export default function Page() {
  return <ResourcesPage />;
}